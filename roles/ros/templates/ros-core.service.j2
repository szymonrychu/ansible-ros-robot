[Unit]
Description=start roscore
After=network.target
After=network.target

[Service]
ExecStart=/bin/bash -c 'source /opt/ros/{{ ros_release }}/setup.bash; roscore'
Restart=on-abort

[Install]
WantedBy=multi-user.target